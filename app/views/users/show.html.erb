<nav class="navbar navbar-expand-md navbar-dark bg-dark">
  <ul class="navbar-nav">
    <li class="nav-item">
      <%= link_to @user.name, user_path(@user), class: "nav-link" %>
    </li>
    <li class="nav-item">
      <%= link_to "New Message", new_message_path, class: "nav-link" %>
    </li>
    <li class="nav-item">
      <%= link_to "Inbox", inbox_path(@user), class: "nav-link load_inbox" %>
    </li>
    <li class="nav-item">
      <%= link_to "Sent", sent_messages_path(@user), class: "nav-link load_outbox" %>
    </li>
    <li class="nav-item">
      <%= link_to "Logout", "/logout", method: :post, class: "nav-link" %>
    </li>
  </ul>
</nav>

<%= @user.name %>




<h4></h4>
<h4>Inbox</h4>
<% @user.received_messages.each do |message| %>
  <p><%= message.sender.name %> | <%= message.subject %></p>
<% end %>
<h4>Sent Messages</h4>
<% @user.sent_messages.each do |message| %>
  <p><%= message.recipient.name %> | <%= message.subject %></p>
<% end %>

<div id="inbox"></div>

<h4></h4>
<div id="outbox"></div>
